# API base URL for the backend LMS service.
# Override per-environment by copying to `.env.local`.
VITE_API_BASE_URL=http://localhost:6505/api

# System config categories shown in the admin UI (must match backend SYSTEM_CONFIG_CATEGORIES)
VITE_SYSTEM_CONFIG_CATEGORIES=general,storage,notifications,loans,integrations,email
