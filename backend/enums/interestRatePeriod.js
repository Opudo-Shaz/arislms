// Interest rate period enum: whether a loan/product's `interestRate` figure is
// expressed as a monthly rate or an annual (yearly) rate.
// Use CommonJS exports to match project style

module.exports = Object.freeze({
  MONTHLY: 'monthly',
  ANNUAL: 'annual',
});
